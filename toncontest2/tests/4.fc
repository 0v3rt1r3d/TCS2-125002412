
[int, tuple, cell, tuple, int] serialize_1_data() method_id(0) {
  tuple input = empty_tuple();
  input~tpush(10);

	tuple stack = unsafe_tuple([input]);
  cell data = begin_cell().end_cell();
  int function_selector = 120880;
  return [function_selector, stack, data, get_c7(), null()];
}

_ serialize1(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(1) {
  throw_if(exit_code, exit_code != 0);
  tuple ret = stack.at(0);
  ret~dump();
}
